name: Deploy to Cloudflare Workers

on:
  workflow_run:
    workflows: ["CodeQL", "Secret Scanning", "Lint and Test"]
    types:
      - completed
    branches:
      - master
  workflow_dispatch:  # Allow manual deployments

jobs:
  deploy:
    runs-on: ubuntu-latest
    # Only deploy if all security checks passed
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    permissions:
      contents: read
      deployments: write  # Required for creating deployments
    
    environment:
      name: production
      url: https://d1.adamic.tech

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build application
        run: npm run build

      - name: Deploy to Cloudflare Workers
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: ${{ secrets.CF_ACCOUNT_ID }}
      
      - name: Deployment Summary
        if: success()
        run: |
          echo "✅ Successfully deployed to production"
          echo "🌐 URL: https://d1.adamic.tech"
          echo "📝 Commit: ${{ github.sha }}"
          echo "👤 Deployed by: ${{ github.actor }}"

