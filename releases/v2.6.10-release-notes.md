---
product: "D1 Manager"
version: "v2.6.10"
date: "2026-05-14"
slug: "d1-manager-v2.6.10"
docker_image: "writenotenow/d1-manager"
github_repo: "neverinfamous/d1-manager"
description: "D1 Manager v2.6.10 updates core dependencies including React 19, Vite 8, and Tailwind 4, and patches Dockerfile security vulnerabilities."
---

# D1 Manager v2.6.10

## Highlights

- **Ensure Enterprise Security**: Patched critical Dockerfile dependencies to protect containerized deployments.
- **Modernize App Ecosystem**: Upgraded React, Vite, and Tailwind dependencies for peak frontend build and runtime performance.

## Changed

- **deps**: Bumped `@cloudflare/workers-types` to `4.20260514.1`.
- **deps**: Bumped `@tailwindcss/postcss` and `@tailwindcss/vite` to `4.3.0`.
- **deps**: Bumped `@types/node` to `25.8.0`.
- **deps**: Bumped `@vitejs/plugin-react` to `6.0.2`.
- **deps**: Bumped `lucide-react` to `1.16.0`.
- **deps**: Bumped `react` and `react-dom` to `19.2.6`.
- **deps**: Bumped `sql-formatter` to `15.7.4`.
- **deps**: Bumped `tailwind-merge` to `3.6.0`.
- **deps**: Bumped `tailwindcss` to `4.3.0`.
- **deps**: Bumped `typescript-eslint` to `8.59.3`.
- **deps**: Bumped `vite` to `8.0.13`.

## Security

- **docker**: Bumped Dockerfile pinned transitive dependency `tar` to `7.5.15` to resolve security advisories.

## Install

```bash
docker pull writenotenow/d1-manager:v2.6.10
```

[Full Changelog](https://github.com/neverinfamous/d1-manager/compare/v2.6.9...v2.6.10)
